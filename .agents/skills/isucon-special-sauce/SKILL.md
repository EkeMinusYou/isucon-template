---
name: isucon-special-sauce
description: docs/special-sources の指定した設定断片、指定なしなら全件を現行設定・コード・公式仕様と照合し、原則として適用候補を出典付きの INVESTIGATE に起票する。適用判断は isucon-investigate に委ね、READY化、実装、設定変更、デプロイ、ベンチは行わない。
---

# ISUCON 秘伝のタレ候補の調査

## 対象を選ぶ

- 指定があれば、`docs/special-sources/` 内のパス、ファイル名、カタログのリンク、一意な名称から対象を解決する。文書指定は文書全体、節・設定項目の指定はその範囲、複数指定は和集合だけを調べる。
- 指定がなければ `docs/special-sources/README.md` と実際のファイル一覧を照合し、ルート README 以外の全 Markdown 文書を調べる。カタログ未掲載の文書も含め、差分を報告する。
- 指定が曖昧または対象が存在しない場合は、対象を確認する。全件調査へ勝手に広げず、対象確定までは起票しない。

## 現行設定と照合して起票する

対象資料の設定を現行設定・コード・公式仕様と照合し、未適用のものを原則として INVESTIGATE に起票する。出典、現行値と提案値の差分、設定の生成元と反映先、未確定点を記録する。適用の必要性・Objectiveへの因果・変更範囲・採否の判断は `isucon-investigate` に委ねる。

適用済み・重複・明確な適用不可の場合は理由を残し、重複は既存カードへ補足する。部分適用なら未反映部分を候補とし、一体で採否・適用・切り戻すと分かる設定群は一枚にまとめる。

起票時の actor は `skill:isucon-special-sauce`。

## 完了報告

指定対象／全件の別と調査文書、現行との差分、起票・補足したカード ID と Objective、出典リンク、見送った理由、未確定点を簡潔に報告する。次の判断は `isucon-investigate` へ引き渡す。
