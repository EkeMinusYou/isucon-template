---
name: isucon-special-sauce
description: docs/special-sources の指定した設定断片、指定なしなら全件を現行設定・コード・公式仕様と照合し、適用候補を出典付きの INVESTIGATE として引き渡す。READY化、実装、設定変更、デプロイ、ベンチは行わない。
---

# ISUCON 秘伝のタレ候補の調査

## 対象を選ぶ

- 指定があれば、`docs/special-sources/` 内のパス、ファイル名、カタログのリンク、一意な名称から対象を解決する。文書指定は文書全体、節・設定項目の指定はその範囲、複数指定は和集合だけを調べる。
- 指定がなければ `docs/special-sources/README.md` と実際のファイル一覧を照合し、ルート README 以外の全 Markdown 文書を調べる。カタログ未掲載の文書も含め、差分を報告する。
- 指定が曖昧または対象が存在しない場合は、対象を確認する。全件調査へ勝手に広げず、対象確定までは起票しない。

## 現行設定と照合する

[既知資料の候補評価手順](../isucon-analyze/references/known-solutions.md) に従う。この手順は本スキルから直接実行し、広範な `isucon-analyze` の探索へ対象を拡張しない。

設定ごとに現行値、文書の提案値、適用条件、設定の生成元と実効先を確認する。nginx、MySQL、systemd、sysctl、Go の DB 接続設定など、対象資料に関係する正本だけを追う。設定例の値を無条件に採用せず、現在の資源上限、役割配置、データ保持・再起動条件と照合する。

部分適用なら未反映部分に独立した改善経路があるか確認する。一体で採否・適用・切り戻す設定群は一枚にまとめる。設定・元資料の編集や適用は行わない。

起票時の actor は `skill:isucon-special-sauce`。

## 完了報告

指定対象／全件の別と調査文書、現行との差分、起票・補足したカード ID と Objective、出典リンク、見送った理由、未確定点を簡潔に報告する。次の判断は `isucon-investigate` へ引き渡す。
